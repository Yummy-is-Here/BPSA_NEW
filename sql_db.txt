CREATE TABLE positions (
  id INT AUTO_INCREMENT PRIMARY KEY,
  nazev VARCHAR(255) NOT NULL,
  lokalita VARCHAR(255),
  popis TEXT,
  obrazek VARCHAR(255),
  datum_pridani DATETIME DEFAULT CURRENT_TIMESTAMP
);
